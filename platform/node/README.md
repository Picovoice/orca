# Node-API for zoo-dev

## Build

Build the zoo-dev repository as normal. The `.node` files will be generated and put in both `build` and `node/build` folder.

## Test

Run the following code in the terminal:

```console
cd node
yarn
yarn test --platform=${PLATFORM} --access_key=${ACCESS_KEY}
```
